name: Super-Linter

on:
  workflow_dispatch:
  pull_request:
  push:

permissions:
  contents: read
  checks: write
  pull-requests: write
  security-events: write  # Required for SARIF upload

jobs:
  super-lint:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📁 Print repo structure
        run: tree -L 3 || echo "tree not installed"

      - name: 📄 List all checked out files
        run: find . -type f

      - name: 🧹 Run Super-Linter (official action)
        uses: github/super-linter@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_JSON: true
          VALIDATE_PYTHON: true
          VALIDATE_JAVASCRIPT_STANDARD: true
          CREATE_LOG_FILE: true
          LOG_LEVEL: VERBOSE
          NO_COLOR: true   # 👈 prevents unreadable ANSI codes

      - name: 📌 Save Linter Errors to File
        if: always()
        run: |
          if [ -f super-linter.log ]; then
            # Extract only real errors (file paths + rules)
            grep -E "standard:|/github/workspace/" super-linter.log \
              | sed -E "s/\x1B\[[0-9;]*[A-Za-z]//g" > super-linter-errors.log
            # If empty, mark no errors
            if [ ! -s super-linter-errors.log ]; then
              echo "No linting errors found 🎉" > super-linter-errors.log
            fi
          else
            echo "No super-linter.log found" > super-linter-errors.log
          fi

      - name: 📦 Upload Super-Linter Logs and Errors
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: super-linter-logs
          path: |
            super-linter.log
            super-linter-errors.log
            super-linter.sarif
          if-no-files-found: ignore

      - name: 📝 Generate Step Summary
        if: always()
        run: |
          echo "### Super-Linter Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "This workflow checks code formatting for:" >> "$GITHUB_STEP_SUMMARY"
          echo "- Python" >> "$GITHUB_STEP_SUMMARY"
          echo "- JavaScript" >> "$GITHUB_STEP_SUMMARY"
          echo "- JSON" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "#### Preview of Linter Errors" >> "$GITHUB_STEP_SUMMARY"
          echo '```text' >> "$GITHUB_STEP_SUMMARY"
          if [ -s super-linter-errors.log ]; then
            head -50 super-linter-errors.log >> "$GITHUB_STEP_SUMMARY"
            if [ $(wc -l < super-linter-errors.log) -gt 50 ]; then
              echo "... (truncated, see full log artifact)" >> "$GITHUB_STEP_SUMMARY"
            fi
          else
            echo "No errors found" >> "$GITHUB_STEP_SUMMARY"
          fi
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Job summary generated at run-time" >> "$GITHUB_STEP_SUMMARY"

      - name: 📊 Upload SARIF report to GitHub Code Scanning
        if: always() && hashFiles('super-linter.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: super-linter.sarif
